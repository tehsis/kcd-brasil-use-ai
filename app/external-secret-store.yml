apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: my-secret-store
spec:
  provider:
    pulumi:
      organization: logmeup
      project: kcd-brasil
      environment: dev-s3-read-only
      accessToken:
        secretRef:
          name: my-pu-secret
          key: pu-at

